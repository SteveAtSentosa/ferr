# Changesets

This folder stores release notes and version bump intents for `ferr`.

Typical local flow:

1. `pnpm create-changeset`
2. `pnpm set-version`
3. `pnpm sanity`
4. `pnpm publish:changeset`
